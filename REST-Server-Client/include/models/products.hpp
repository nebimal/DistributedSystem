#pragma once

#include <string>

struct Product
{
    int id;                     // Product's Unique Identifier Number
    double price;               // Product's Price
    std::string name;           // Product's Name
    std::string description;    // Product's Description
    std::string image;          // Product's Image
};
